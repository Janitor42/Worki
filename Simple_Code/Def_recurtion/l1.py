1
# Напиши рекурсивную функцию count_down(n),
# которая принимает число n и печатает на экране числа от n до 1, а затем печатает "Старт!".
# 5
# 4
# 3
# 2
# 1
# Старт!

# def count_down(n):
#     if n >= 1:
#         print(n)
#         count_down(n - 1)
#     else:
#         print('start')
#
#
# count_down(n=5)

2
# Задание на закрепление (Раунд 2)
# Напиши рекурсивную функцию sum_numbers(n), которая считает сумму чисел от 1 до n.

# sum_numbers(5) → 15 (потому что 1+2+3+4+5 = 15)
# sum_numbers(3) → 6 (1+2+3)

# Подсказка: Функция должна возвращать результат, а не печатать его

# def sum_numbers(n):
#     if n == 1:
#         return 1
#     return sum_numbers(n - 1) + n
# print(sum_numbers(3))
3
# Задание:
# Напиши рекурсивную функцию power(base, exponent), которая возводит число в степень.
# exponent — целое неотрицательное число (0, 1, 2, 3...).
# Нельзя использовать оператор ** или math.pow.
# Нужно использовать именно рекурсию.

# Примеры:
# power(2, 3) → 8 (2 * 2 * 2)
# power(5, 0) → 1 (любое число в степени 0 = 1)
# power(3, 1) → 3

# Вспомни математику:
# base^exponent = base * base^(exponent-1)
# Базис: если exponent == 0, возвращаем 1.

# def power(base, exponent):
#     if exponent == 0:
#         return 1
#     return power(base,exponent-1)*base
# print(power(3, 3))

4

# Напиши рекурсивную функцию list_sum(arr),
# которая принимает список чисел и возвращает их сумму.
# Нельзя использовать встроенную sum().
# Нельзя использовать циклы (for, while).
# Только рекурсия.

# list_sum([1, 2, 3, 4, 5]) → 15
# list_sum([10, -5, 3]) → 8
# list_sum([]) → 0 (пустой список)

# Базис: пустой список → сумма 0.
# Шаг: первый элемент + сумма оставшегося списка.
# Оставшаяся часть списка — это arr[1:].

# def list_sum(arr):
#     if len(arr) == 0:
#         return 0
#     return arr[0] + list_sum(arr[1:])
#
#
# print(list_sum([1, 2, 3, 4, 5]))

5

# Напиши рекурсивную функцию even_numbers(arr),
# которая принимает список целых чисел и возвращает новый список,
# содержащий только чётные числа из исходного.
#
# Условия:
# Не использовать циклы.
# Не использовать встроенные функции типа filter.
# Возвращать именно список (даже пустой).

# even_numbers([1, 2, 3, 4, 5, 6]) → [2, 4, 6]
# even_numbers([1, 3, 5]) → []
# even_numbers([]) → []

# Если список пуст → возвращаем пустой список.
# Если первый элемент чётный → включаем его в результат и добавляем к нему результат обработки остатка списка.
# Если нечётный → просто возвращаем результат обработки остатка (без текущего элемента).
# Синтаксис: можно собирать список так: [arr[0]] + even_numbers(arr[1:]) (если чётное).


# def event_numbers(arr):
#     if len(arr) == 0:
#         return []
#     if arr[0] % 2 == 0:
#         return [arr[0]] + event_numbers(arr[1:])
#     return event_numbers(arr[1:])
#
#
# print(event_numbers([1, 2, 3]))

"""визуальное обьяснение 5"""
# even_numbers([1, 2, 3])
#  └→ 1 нечётный → просто even_numbers([2, 3])
#      └→ 2 чётный → [2] + even_numbers([3])
#          └→ 3 нечётный → even_numbers([])
#              └→ [] (базис)
#          ← получаем [] → [2] + [] = [2]
#      ← получаем [2] → просто [2]
# ← получаем [2]

6

# Напиши рекурсивную функцию list_reverse(arr),
# которая принимает список и возвращает новый список с элементами в обратном порядке.

# Никаких срезов с шагом -1 (arr[::-1] — это читерство ❌)
# Никаких циклов
# Никаких встроенных reverse()

# list_reverse([1, 2, 3, 4]) → [4, 3, 2, 1]
# list_reverse([5]) → [5]
# list_reverse([]) → []

# Поставить первый элемент в конец результата от переворота остатка.
# Или поставить последний элемент в начало результата от переворота начала.


# def list_reverse(arr):
#     if len(arr) == 0:
#         return []
#
#     x = arr[0]
#     return list_reverse(arr[1:]) + [x]
#
#
# #
# print(list_reverse(arr=[1, 2, 3, 4]))

"""визуальное обьяснение 6"""
# list_reverse([1, 2, 3, 4])
#   → list_reverse([2, 3, 4]) + [1]
#       → list_reverse([3, 4]) + [2] + [1]
#           → list_reverse([4]) + [3] + [2] + [1]
#               → list_reverse([]) + [4] + [3] + [2] + [1]
#                   → [] + [4] + [3] + [2] + [1]
#               ← [4]
#           ← [4, 3]
#       ← [4, 3, 2]
#   ← [4, 3, 2, 1]

7


# Задача с привкусом ЕГЭ (но без паники)
# Напиши рекурсивную функцию count_ways(n), которая считает,
# сколькими способами можно подняться по лестнице из n ступенек,
# если за шаг можно перешагивать либо на 1, либо на 2 ступеньки.

# count_ways(1) → 1 способ: [1]
# count_ways(2) → 2 способа: [1,1] или [2]
# count_ways(3) → 3 способа: [1,1,1], [1,2], [2,1]
# count_ways(4) → 5 способов (угадай почему)

# Это та самая последовательность Фибоначчи, но со смещением.
# Чтобы попасть на ступеньку n, можно прийти с n-1 (сделав шаг 1) или с n-2 (сделав шаг 2).
# Значит: ways(n) = ways(n-1) + ways(n-2)
# Базис:
# n == 1 → 1 способ
# n == 2 → 2 способа

# def count_ways(n):
#     if n == 1:  # базис 1
#         return 1
#     if n == 2:  # базис 2
#         return 2
#     x = count_ways(n - 1)
#     x += count_ways(n - 2)
#     return x
#
#
# print(count_ways(n=5))

# с использованием словаря для использования уже найденых значений
# def count_ways(n, memo={}):
#     if n in memo:
#         return memo[n]
#
#     if n == 1:
#         return 1
#     if n == 2:
#         return 2
#
#     memo[n] = count_ways(n - 1) + count_ways(n - 2)
#     return memo[n]
#
#
#

# 165580141
"""визуальное обьяснение 7"""
# count_ways(4) = count_ways(3) + count_ways(2)
#               = (count_ways(2) + count_ways(1)) + 2
#               = (2 + 1) + 2
#               = 5
# это решение уже тянет на 1 балл в ЕГЭ (задание 23, если там попадутся такие лесенки). Но есть нюанс...

8

# ЕГЭ-style (та же задача, но с подвохом)(почти как в реальном ЕГЭ)
# У исполнителя есть две команды:
# Прибавь 1
# Умножь на 2
# Сколько существует различных программ, которые число 1 преобразуют в число 10?

# Условия:
# Траектория вычислений не должна содержать число 5.
# То есть в процессе выполнения программы (в промежуточных результатах) число 5 встречаться не должно.

# Пример: программа 1 → 2 → 4 → 8 → 9 → 10 не подходит?
# Смотрим: 1 (ок), 2 (ок), 4 (ок), 8 (ок), 9 (ок), 10 (ок). Числа 5 нет — значит, подходит! ✅

# Напиши функцию count_ways_ege(start, target), где:
# start — текущее число (начинаем с 1)
# target — целевое число (10)
# Нужно вернуть количество программ

# Базисы:
# Если start > target → 0 (переборщили)
# Если start == target → 1 (достигли цели)
# Если start == 5 → 0 (запретное число)

# result = []
#
#
# def count_ways_ege(start, target):
#     if start == 5 or start > target:
#         return 0
#
#     if start == target:
#         result.append(1)
#         return 0
#
#     return count_ways_ege(start + 1, target) + count_ways_ege(start * 2, target)
#
#
# count_ways_ege(1, 40)
# print(len(result))

9

# ЕГЭ-23 (чуть сложнее, чем прошлая)
# Задание:
# У исполнителя есть три команды:
# Прибавь 1
# Прибавь 2
# Умножь на 3
# Сколько существует различных программ, которые число 1 преобразуют в число 20?

# Дополнительное условие (самое интересное):
# Траектория вычислений не должна содержать число 10.
# Пояснение:
# Траектория — это все промежуточные результаты, включая начальный (1) и конечный (20).
# Число 10 в траектории запрещено (нельзя ни быть в 10, ни проходить через него).

# Напиши рекурсивную функцию count_ways_ege2(start, target),
# которая возвращает количество программ, удовлетворяющих условию.
#
# Базисы:
# Если start > target → 0
# Если start == target → 1
# Если start == 10 → 0 (запрет)

# result = 0
#
#
# def count_ways_ege2(start, target):
#     global result
#     if start > target:
#         return 0
#     if start == target:
#         return 1
#     if start == 10:
#         return 0
#     return count_ways_ege2(start + 1, target) + count_ways_ege2(start + 2, target) + count_ways_ege2(start * 3, target)
#
#
# print(count_ways_ege2(start=1, target=20))

10
# Задача 16.1 (Классика из демоверсии)
# Условие:
# Алгоритм вычисления функции F(n) задан следующими соотношениями:

# F(n) = n + 3, при n < 4
# F(n) = F(n-3) + 2*F(n-1), при n >= 4

# def F(n):
#     if n<4:
#         return n+3
#     return F(n-3)+2*F(n-1)
#
# print(F(6))


"""визуальное обьяснение 10"""
# Разберём построчно (очень важно!)
# Строка 1: def F(n):
# Объявляем функцию с именем F, которая принимает один аргумент n.
#
# Строка 2: if n < 4:
# Это базис (условие остановки).
# Если n меньше 4, мы НЕ вызываем функцию снова, а просто считаем по формуле.
#
# Строка 3: return n + 3
# Если n < 4, возвращаем n + 3.
# Это соответствует первому условию из задачи:
#
# F(n) = n + 3, при n < 4
#
# Строка 4: return F(n - 3) + 2 * F(n - 1)
# Если n >= 4, то мы вызываем эту же функцию, но с другими аргументами:
#
# Первый вызов: F(n - 3) — считаем значение для числа на 3 меньше
#
# Второй вызов: F(n - 1) — считаем значение для числа на 1 меньше
#
# Умножаем второй результат на 2 и складываем
#
# Это соответствует второму условию:
#
# F(n) = F(n-3) + 2*F(n-1), при n >= 4


# F(6) = F(3) + 2*F(5)
#         ↓        ↓
#       n<4 → 6   F(5) = F(2) + 2*F(4)
#         ↑        ↓        ↓
#       вернёт 6   n<4 → 7   F(4) = F(1) + 2*F(3)
#                  ↑               ↓        ↓
#                вернёт 5         n<4 → 4   n<4 → 6
#                                вернёт 4


#
# a = 1
# b = 16
# all_path = []
# all_simbols=[]
#
# def recursion(a, b, simbol=None):
#     if simbol==None:
#
#         simbol=[]
#     if a == b:
#         all_simbols.append(simbol.copy())
#         return 1
#     if a > b:
#         return 0
#     x = recursion(a+1, b, simbol+['+'])
#     x += recursion(a*2, b, simbol+['*'])
#     return x
#
#
# recursion(a, b)
# print(all_path)
# print(all_simbols)
