1. gui интерфейс с обработкой недопустимых данных
2. подключение к gui requests и встройка поиска алгоритма
3. сохранение данных в json
4.  кнопка load  если есть что загружать загрузка из json обратно в gui (новые окна) каскадом (без фотографии)


** загрузка в режиме реального времени (используем threading)
*** в досье так же есть  фото человека (используем pill) (фоточки тоже сохраняются)
